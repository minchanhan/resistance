class Game {
  constructor(mission, results) {
    this.mission = mission; // int (a.k.a round)
    this.results = results; // arr[string]
  }

  startGame() {
    // start game

    // randomize teams

    // randomize leader


    // start mission 1

    for (let index = 0; index < 5; index++) {
      const element = array[index];
      
    }
  }




}